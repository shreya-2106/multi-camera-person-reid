# Project dependencies

PyYAML>=6.0
opencv-python>=4.8

# Phase 3 - YOLO person detection
ultralytics>=8.1

pytest>=7.4
pytest-cov>=4.1

# Optional ML acceleration.
# Install a suitable PyTorch build separately if GPU acceleration is required.
# torch>=2.0
